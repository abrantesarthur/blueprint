# Port `dev:down` uses to find and kill the Next.js dev listener.
# Must match the port `next dev` actually binds (default 3000). If you
# change this, also pass `-p <port>` to `next dev` in package.json.
PORT=3000
