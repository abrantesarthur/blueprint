# Specification

This file holds the spec for the feature currently being worked on. It is
intentionally committed as a placeholder and marked `skip-worktree` by the
root `postinstall` script (`git update-index --skip-worktree .claude/specs/spec.md`),
so local edits to it are never picked up by git.

Use the `/spec` skill (see `.claude/skills/spec/`) to generate a spec here
before planning and implementing a feature.
